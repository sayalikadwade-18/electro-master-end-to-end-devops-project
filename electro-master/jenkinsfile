pipeline {

    agent any

    environment {
        SONAR_PROJECT_KEY = "electro-master"
        REMOTE_HOST = "ubuntu@43.204.143.201"
    }

    stages {

        stage('Checkout Code') {
            steps {
                git branch: 'main',
                    credentialsId: 'sayalikadwade-18',
                    url: 'https://github.com/sayalikadwade-18/electro-master-end-to-end-devops-project.git'
            }
        }

        stage('SonarQube Analysis') {
            steps {
                echo 'Running SonarQube Analysis'

                withSonarQubeEnv('EM-sonar-srv') {
                    script {
                        def scannerHome = tool 'EM-sonarscanner'

                        sh """
                        ${scannerHome}/bin/sonar-scanner \
                        -Dsonar.projectKey=${SONAR_PROJECT_KEY} \
                        -Dsonar.projectName=${SONAR_PROJECT_KEY} \
                        -Dsonar.sources=. \
                        -Dsonar.sourceEncoding=UTF-8
                        """
                    }
                }
            }
        }

        stage('Deploy To Docker Server') {
            steps {

                sshagent(['docker-creds']) {

                    sh """
                    ssh -o StrictHostKeyChecking=no ${REMOTE_HOST} '
                        mkdir -p ~/website
                    '

                    rsync -av --delete \
                    --exclude=".git" \
                    --exclude=".scannerwork" \
                    ./ ${REMOTE_HOST}:~/website/

                    ssh -o StrictHostKeyChecking=no ${REMOTE_HOST} '
                        cd ~/website

                        docker rm -f electro-master1 || true

                        docker build -t mywebsite .

                        docker run -d \
                            --name electro-master2 \
                            -p 8086:80 \
                            mywebsite

                        docker ps
                    '
                    """
                }
            }
        }
    }

    post {
        success {
            echo 'Deployment Successful'
        }

        failure {
            echo 'Deployment Failed'
        }
    }
}
